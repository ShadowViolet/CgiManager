# 开启调试模式 (true/false)
debug=true

# 错误显示信息,非调试模式有效
error_message=页面错误！请稍后再试～

# 日志保存目录
log_path=log

# 默认编码 (留空将使用系统默认编码)
charset=

# 默认输出类型
return_type=text/html

# 默认AJAX 数据返回格式,可选json xml
ajax_return=json

# 控制器类前缀 (不使用前缀则为空)
controller_prefix=C

# 控制器类后缀 (不使用后缀则为空)
controller_suffix=

# 默认控制器名
default_controller=CIndex

# 默认操作名
default_action=index

# 操作方法后缀 (不使用后缀则为空)
action_suffix=

# pathinfo分隔符 (没有特殊需求最好别改)
pathinfo_depr=/

# URL伪静态后缀
html_suffix=html

# 是否开启路由 (true/false)
url_route=true

#cookie 过期时间 (86400秒为一天)
cookie_expire=86400

# cookie 保存路径
cookie_path=/

# cookie 有效域名
cookie_domain=localhost